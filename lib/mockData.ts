// Reference data — kept for development / future seeding.
// The Social Media page no longer loads from here.
// Only `creators` is exported and used as the initial creator list seed.

import { Creator } from "@/types/instagram"

export const creators: Creator[] = [
  { id: "gina",   name: "Gina"   },
  { id: "romina", name: "Romina" },
]
